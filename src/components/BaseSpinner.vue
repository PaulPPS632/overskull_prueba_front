<template>
  <div class="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/40 backdrop-blur-sm">
    <div class="flex items-center gap-3 rounded-xl bg-white px-5 py-4 shadow-2xl">
      <span class="h-5 w-5 animate-spin rounded-full border-4 border-emerald-500 border-t-transparent"></span>
      <span class="text-sm font-semibold text-slate-800">Cargando...</span>
    </div>
  </div>
</template>
